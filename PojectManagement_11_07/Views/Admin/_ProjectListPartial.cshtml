@model List<Projects>
@foreach (var project in Model)
{
    <div class="col-xl-3 col-md-6 team-box fixed-size" id="@project.ProjectId">
        <div class="card fixed-size-card">
            <div class="card-body">
                <div class="d-flex mb-1">
                    <div class="flex-grow-1 align-items-start">
                        <h6 class="mb-0 text-muted">
                            <i class="mdi mdi-circle-medium text-success fs-3 align-middle"></i>
                            <span class="team-date">@project.StartDate</span>
                        </h6>   
                    </div>
                    <div class="dropdown ms-2">
                        <a href="#" class="dropdown-toggle font-size-16 text-muted" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="mdi mdi-dots-horizontal"></i>
                        </a>
                        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
                            <a asp-action="EditProject" asp-route-id="@project.ProjectId" class="dropdown-item">Sửa</a>
                            <a asp-action="Task" asp-controller="Task" asp-route-projectId="@project.ProjectId" class="dropdown-item">Xem Nhiệm Vụ</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item delete-item" type="submit" onclick="deleteProject(@project.ProjectId)" data-id="project-items-8" href="javascript:void(0);">Xóa</a>
                        </div>
                    </div>
                </div>
                <div class="mb-4">
                    <h5 class="mb-1 font-size-17 team-title">@project.ProjectName</h5>
                    <p class="text-muted mb-0 team-description">@project.ProjectDescription</p>
                </div>
                <div class="d-flex">
                    <div class="avatar-group float-start flex-grow-1 task-assigne">
                        <img src="https://cdn.icon-icons.com/icons2/3053/PNG/512/trello_macos_bigsur_icon_189616.png" class="rounded-circle avatar-sm" data-bs-toggle="tooltip" data-bs-placement="top" title="Elias Hardage">
                        <span class = "text-muted">SL: @project.ProjectUsers.Count</span>
                    </div>
                    <span class="custom-badge 
                        @if (@project.StatusProject == "Đang thực hiện") { 
                            <text>custom-badge-progress</text> 
                        } else if (@project.StatusProject == "Hoàn thành") { 
                            <text>custom-badge-completed</text> 
                        }">
                        @project.StatusProject
                    </span>
                </div>
            </div>
        </div>
    </div>
}
